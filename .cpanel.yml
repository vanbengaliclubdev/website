---
deployment:
  tasks:
    - export DEPLOYPATH=/home3/vascoshospitality/web.vancouverbengaliclub.com/
    - /usr/bin/find . -mindepth 1 -maxdepth 1 ! -name '.git' -exec /bin/cp -R {} "$DEPLOYPATH" \;
